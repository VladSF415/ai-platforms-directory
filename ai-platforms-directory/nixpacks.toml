# Nixpacks configuration for Railway deployment
# This ensures git is available in the runtime container for auto-sync

[phases.setup]
aptPkgs = ["git"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]
